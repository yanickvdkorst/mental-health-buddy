import SwiftUI

struct NewFeatureView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("New Feature")
                .font(.largeTitle)
                .padding()
        }
        .padding()
    }
}

#Preview {
    NewFeatureView()
}
